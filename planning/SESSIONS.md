# SESSIONS.md — Write Ledger for course-cts285-storming
*Anti-fork discipline: every session that writes to this repo appends a row here (part of its final commit). New sessions read this first. See CLAUDE_SUPPLEMENTARY.md Rules 4–5 for the workflow this feeds.*

## Rules of the ledger

1. **One writer at a time.** The WRITE TOKEN row below names who may commit. Everyone else is read-only until the token passes (by editing that row in a commit).
2. **Push early, pull first.** Push after every writing session; pull before starting one. `origin/main` is the meeting point. Cloud (bridge) sessions cannot push — a human or local session must push their commits.
3. **Every commit carries a session trailer** (`Claude-Session: <url>` for Claude sessions; Claude Code adds its own `Co-Authored-By`). Distinct committer names per actor: `Teacherbot` = cloud Cowork sessions · Claude Code default identity = local agent sessions · your own git identity = human commits.
4. A session that ends mid-task leaves a handoff block (Rule 5, CLAUDE_SUPPLEMENTARY) in its PR or issue, and still appends its ledger row.

## WRITE TOKEN

**Holder:** Cloud Claude Code — session `session_01R4yvjTL3XyKCkTPfMcGqiB`.
**Passed from:** Drew (human), 2026-07-31 — *"you have the token, do B-024,"* re-affirmed at the merge of PR #29: *"I merged 29 just handing you the token."*
**Held until:** Drew takes it back, Drew names another holder, or this session ends. **Merging is not a return event.** A holder line names a session, never a branch.

> **Correction (2026-07-29):** the shipped version of this file read "*pending handoff to local Claude Code for Phase 1*." That was accurate when the sync package was built (baseline `ca36b45`) but stale on arrival — Phase 1 had already run to 12 merged PRs. See the backfill block below.

> **Correction (2026-07-31):** this block named Cloud Claude Code session `session_015ox6hTidX21c2tnV4xT2Jo` (branch `claude/algocratic-sync-assessment-8fwtzn`) as holder, passed from Drew 2026-07-29 — *"session that built zip is archived, you have the lead currently."* That branch merged at `cac6def`, so by the entry's own terms the token had already returned to Drew and the holder line was stale. The B-003 session measured this, declined to self-grant, and flagged it as BLOCKED-ON in PR #28; Drew ruled on 2026-07-31. **Recorded, not backdated:** the 47 B-003 edits (`3abc5f4`) were applied while the token was unheld, on a branch behind a draft PR. The reviewable path stood in for the token; it does not retroactively confer it.
>
> **Standing note for the next session:** a holder line that names a branch also names its own expiry. When that branch merges, the token is Drew's again and the line is stale until someone says otherwise — read the merge state of the named branch before trusting the holder.

> **Correction (2026-07-31, later) — the standing note above is WRONG and is superseded.** It tied the token's expiry to a branch merging. Under the Trusted Workflow every task ends in a merge, so that rule returns the token after *every* task and makes holding it across a sequence of tasks impossible without a fresh grant each time. Merging is how work lands; it is not a transfer of authority. Drew named the error directly at the merge of PR #29 — *"I merged 29 just handing you the token"* — confirming the merge was not a return.
>
> **The rule that replaces it:** the token is held by a **session**, not a branch, and it is held until the human takes it back, names another holder, or the session ends. A holder line that names a branch is malformed; name the session and let the branch be incidental.
>
> Both entries are kept. The wrong rule survived one merge cycle before it was caught, and that is the interval this ledger exists to make visible.

> 🗃️ **Correction (2026-08-07) — the 🔑 no longer exists.** Instructor ruling: the token was redacted
> out of the logs and is gone; it can be re-established if it is ever needed. The holder line above
> and all three corrections beneath it are **kept exactly as written** — they are the record of how
> the token was held and mis-held, and that history is the reason this block is legible at all.
>
> **What this changes.** There is no 🔑 to hold, take back, or pass. A session cannot be blocked on
> it and cannot claim it. Ledger rows should stop reporting 🚫-not-held as though a holdable thing
> existed; from this row forward the field is simply **absent**.
>
> **What it does not change.** The 🔑 was a *write-serialization* device — one writer at a time. It
> was never the authorization device. **Authorization is 🔒 CLEAR** (`ADR-009`), it is the
> instructor's alone, and it is untouched by this. Losing the token removes a queue, not a gate.
>
> **If it is re-established:** name a **session**, never a branch (the rule two corrections above),
> and reduce the handle rather than republishing a working one — the block above republishes a full
> session identifier, which is the defect `F-005` §3 and the `semantics-preserved-abstract` skill
> both flag. That fix was proposed on PR #53 and deliberately not applied, because an unheld session
> editing the holder line is the self-grant these corrections exist to prevent. **Retirement, ruled
> by the instructor, is the legitimate moment — so the correction lands here as a new block rather
> than as an edit above.**
>
> **Converges with the proposed abstract filed on 2026-08-06** (ledger row, same day), which set its
> own trigger as *"the 🔑 passes to a named session, **or a human authorises editing the block**."*
> The second condition is what the retirement ruling supplies. Two sessions reached the same
> mechanism independently and neither applied it early — which is the discipline working, not a
> coincidence worth trusting twice.

## Ledger

| Date (ET) | Session type | Actor / identity | Session link | Commits | Summary |
|---|---|---|---|---|---|
| 2026-07-22 | Cloud Cowork (bridge) | Teacherbot | https://claude.ai/code/session_01NQP3Y6E6nhCZyPN9jcUSU7 | a34e976 | Master plan v1.1 + Phase 0 artifacts (points table, SHODANN bible, ADR-001, PRISM map, naming canon, QM retirement) |
| 2026-07-22 | Cloud Cowork (bridge) | Teacherbot | (same session) | 2f226ca | Proposed project instructions + voice canon exported from Claude project (`reference/`) |
| 2026-07-22 | Cloud Cowork (bridge) | Teacherbot | (same session) | ca36b45 | CLAUDE.md + CLAUDE_SUPPLEMENTARY.md handoff files; KAYFABE_ARCHITECTURE; bible v1.1 (§11) |
| 2026-07-29 | Cloud Cowork (bridge offline → manual package) | Teacherbot, applied by Drew | (same session) | *(this commit)* | Fork consolidation: instructions doc marked ADOPTED w/ instructor amendments (Trusted-Workflow gloss, UV-streamer gloss, past-GREEN scope rule); PRISM map updated to match; SESSIONS.md created; ledger discipline added to CLAUDE_SUPPLEMENTARY |

*(Append new rows above this line — newest last. Never rewrite old rows; corrections get their own row.)*

## Backfill — Phase 1 wave (reconstructed 2026-07-29)

*These sessions predate the ledger and were never logged live. Rows are reconstructed from `git log ca36b45..b937b94` (35 commits, 12 merged PRs) — provenance is git history, not a session record. Kept separate from the ledger proper so nothing above is rewritten.*

| Date (ET) | Session type | Actor / identity | PRs | Commits | Summary |
|---|---|---|---|---|---|
| 2026-07-23 | Local Claude Code | teacherbot | #10 | `a4546ab`, `9e69e97` | Task 0.8 — ingest 250 curated source files into `sources/`; seed `INGESTION_MANIFEST.md` |
| 2026-07-23 | Local Claude Code | teacherbot | #11 | `c419d4d`, `c23f897` | Task 1.0b — points normalization sweep → 750 regime; raised and accepted **ADR-002** (points bind at header level) |
| 2026-07-23 | Local Claude Code | teacherbot | #12 | `650f5a7`, `3e92c6e`, `c83d5d0`, `9b1e34c` | Dataman historical manual transcript (pp. 1–6, 21–26); DataMon reskin spike; NULL villain sharpened to "the value that is not a number" |
| 2026-07-23 | Local Claude Code | teacherbot | #13 | `f2ef009`, `30d2742` | Task 1.0a — shared Dataman class artifacts (3 stakeholder transcripts, reference ERD, seeded backlog); raised and accepted **ADR-003** (graduated stakeholder model) |
| 2026-07-24 | Local Claude Code | teacherbot | #19 | `ccc98e8`, `fa1261a`, `2dcf68e` | Task 1.0c (Weeks 01/07/11 points basis); NAMING_CANON records the 2026 DataMon reskin sense (#16); master plan reconciled with executed work + ADR-002/003 (#14) |
| 2026-07-24 | Local Claude Code | teacherbot | #20 | `6553704` | **ADR-004** (Dataman/DataMon dual modernization basis) + **ADR-005** (prerequisite baseline) |
| 2026-07-24 | Local Claude Code | teacherbot | #21 | `e9ac163` | Task 1.8 (KC half) — adapt M2–M6 knowledge checks to the Dataman regime |
| 2026-07-24 | Local Claude Code | teacherbot | #22 | `fec3526` | Task 1.4 — re-anchor M2 assignments (Weeks 03, 04) to Dataman |
| 2026-07-24 | Local Claude Code | teacherbot | #23, #24 | `ae2f0e2`, `abedb46` | Task 1.5 — re-anchor Week 05 (Sprint 1 System Design) and Week 07 (Sprint 2 Build & Facilitation) to Dataman |
| 2026-07-24 | Local Claude Code | teacherbot | #25 | `e560934`, `ddfeb01`, `d00fb1e` | Task 1.5 — adapt all 6 planning-sheet templates + wiring manifest; instructor directive: Andrew → Teacherbot |
| 2026-07-29 | GitHub Copilot agent | copilot-swe-agent[bot] | #26 | `07c970d`, `e3b2e76` | `REPOSITORY_INVENTORY_AND_PRIORITIZED_BACKLOG.md` — file-tree inventory, source map, dependency-ordered backlog |
| 2026-07-29 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | Ingested `algocratic-26fa-sync.zip`; per-file fitness assessment (4 GO / 3 NO-GO / 15 no-op); applied the GO set; recorded the assessment in `alignment_ingestion/ASSESSMENT.md` |
| 2026-07-29 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **B-002** — consolidated the case-variant draft paths into `drafts/cts285/` (7 planning sheets moved, git renames, history preserved); ruled the repo-internal reference form (repo-root-relative, no leading slash); took the WRITE TOKEN. Course-repo PATHFORM deliberately left open |
| 2026-07-31 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **B-003** — applied the reconciliation worklist. Edits expected: 47 across 7 files. Applicators dispatched: 5, returned: 5, died: 0; applied per applicators: 47/47. Independent audit (re-derived from the diff): 47/47, matching the applicators' count — master plan 22/22, CLAUDE.md 10/10, README.md 3/3, sources/README.md 2/2, INGESTION_MANIFEST.md 1/1, ASSESSMENT_NOTES.md 6/6, WIRING_MANIFEST.md 3/3 (incl. item 47's required paired BLOCKED-ON retirement). Skipped: none; unreached-by-death: none. Only the 7 target files touched; none of the 20 refuted findings or 4 out-of-scope items applied. Audit and applicator counts agree at 47/47 — no shortfall |
| 2026-07-31 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **B-024** — ruled in `phase0/ADR-008-rule-1a-enforcement.md`: enforce Rule 1a by mechanism where it can refuse, keep prose where it cannot. Built `.claude/workflows/bounded-fanout.mjs` (refuses over cap, per-artifact argument shape only, refuses two agents claiming one file, unconditional dispatched-vs-returned) and two agent types (`adversarial-verifier`, `mechanical-propagation`). Declined with reasons: checklist-form authoring skill, `continuity-reviewer` definition. Rule 1a amended to separate its enforced clauses from its advisory ones. **The open question is not closed** — ADR-008 records the probe that would settle it. Token taken from Drew this session; returns on merge |
| 2026-07-31 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Ledger fix** — the WRITE TOKEN expiry rule added earlier this session tied the token to a *branch merging*. Under the Trusted Workflow every task ends in a merge, so that rule returned the token after every task. Corrected: the token is held by a **session** until the human takes it back, names another holder, or the session ends; merging is not a return event. The wrong rule is kept above its correction, per this ledger's own convention |
| 2026-07-31 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **ADR-008 correction + ADR-009 proposal** — fixed the cap hole in `.claude/workflows/bounded-fanout.mjs`: the cap was caller-supplied, so `args.cap` could defeat the refusal. `RULE_1A_CAP = 7` is now a constant; `args.cap` may only lower it; a raise is ignored, logged, and reported as `cap_raise_attempted`. Defect recorded in ADR-008 rather than quietly patched. Raised **ADR-009 (Proposed, not ruled)** — a named human gate (CLEAR) between REVIEW and MERGE, reopening the item ADR-007 declined as out of scope, on field data from three cycles (#28/#29/#30) where the gate was exercised unnamed and REVIEW was skipped 3/3. ADR-009 depends on KEEP Q4 (which human may clear) and must not be frozen before Q4 is answered |
| 2026-07-31 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **ADR-007 ratified + ADR-009 ruled** (instructor: *"ratify ADR-007 and rule ADR-009 … stamped"*). Trusted Workflow canon is now eight steps: **FILE → BRANCH → DRAFT → BUILD → FINISH → REVIEW → CLEAR → MERGE**. Both rulings landed in one commit so canon was never half-converted. Lexicon swept across 6 live files (9 replacements) + `NAMING_CANON.md` verb table + Rule 4 list + Rule 6 gains criterion 7 (an unREVIEWed PR cannot be CLEARed). **No graded answer changed** — the sequence appears in KC explanation prose and one reference line only, never as a correct answer. Records deliberately excluded from the sweep: `sources/`, `alignment_ingestion/`, `reference/voice-canon/`, dated planning logs, and ADRs quoting the old form as history. CLEAR's signer scoped to the instructor; student-held gate authority remains blocked on `the-algorithm` KEEP Q4 |
| 2026-07-31 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Institution name defect** — instructor flagged the expansion of FTCC in `drafts/cts285/` as wrong. Traced: **inherited, not invented here** — the wrong name is in the frozen `sources/uv-285/` base (COURSEMAP, README, INSTRUCTOR_GUIDE) and was carried into 2 live drafts. `reference/PRISM.html` and `sources/uv-289/**` carry a *different* college name. Live drafts now read **FTCC** with a `<!-- NAME: -->` marker; no expansion written, per the instructor's redaction. `sources/` left untouched as the frozen record of what the base actually said. **Open for a human:** the full legal name, and whether it appears in student-facing material at all |
| 2026-08-01 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Q4 answered — `phase0/ADR-010-gate-authority-scoping.md` (Proposed)**. Rule: the signer is whoever bears the consequence; scope by blast radius, not rank — belief 7 read in reverse. No countersignature (two gates, split by sandbox vs. boundary); gate color already entailed by PRISM × SPECTRUM §5/§6, so a CTS-285 student holds no spawn gate by arithmetic; the CLEAR record is evidence, not the grade (K6). One row per K17 customer type. **Proposed, not Accepted** — Rule 8 keeps students, grades, and privacy with a human. Two items flagged and deliberately unruled: education-record status if the CLEAR record is graded, and revocation. General form filed upstream to `the-algorithm` as a SPECTRUM §9 filing (PR norrisaftcc/the-algorithm#28), course detail stripped. `ADR-009` and `NAMING_CANON.md` untouched by design |
| 2026-08-01 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **B-004** (master-plan tasks 1.1 + 1.2) — rewrote `Week_01` around the Dataman spine (Maria scenario replaced; Part 3 optional-bonus deleted per the points table; setup is configure-and-verify per **ADR-005**; analyst read added from the manual transcript; four Dataman-specific struggle patterns; eight-verb Trusted Workflow with **CLEAR performed by the instructor, never the student** per ADR-009). Header 25 pts unchanged; rubric rebalanced to sum to 25. Retired punitive INFRARED block **rewritten, not migrated** (bible §6/§9); Andrew's Note carried over verbatim. NEW `Module_01_Dataman_Document_Analysis_Quiz.md`, 15 pts, 12 autograded items, 2 attempts, no true/false or multi-answer or free text; every item answerable from transcribed pp. 1–6 and 21–26 only (pp. 7–18 untranscribed, issue #18). Salvaged the three analyst-instinct items from the retired M01 KC; dropped its generic SDLC/Agile items. Single session, no fan-out — Rule 1 spine + Rule 2 voice-sensitive |
| 2026-08-06 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **CLAUDE.md refresh** — brought the agent-onboarding files up to the tree's actual state. `CLAUDE.md` gains: ADR-010 in the read-first list (Proposed; until ruled, ADR-009's instructor-only CLEAR stands), the backlog and `SESSIONS.md` as read-first entries (work now dispatches by **B-ID**), a repo-layout table naming `alignment_ingestion/`, `.claude/agents/`, and `bounded-fanout.mjs`, two new non-negotiables (**#8 records are not status**, **#9 institution name = the initialism only**), a "Before you write" section (WRITE TOKEN → eight-verb workflow → handoff block → Rule 1a cap), a current-state snapshot regenerated from the tree, the week-pair numbering trap (no `Week_08`/`Week_10` by design), the transcript's page limits, and the four human blockers that were tracked only in the backlog. Non-negotiable #7 gains the `points-normalization/` and `drafts/spikes/` paths. `CLAUDE_SUPPLEMENTARY.md` status section updated: Phase 1 IN PROGRESS, first wave done, B-005→B-006→B-007 remaining, `drafts/cts285/**` still unswept. **No canon changed** — every statement traces to `phase0/`, the backlog, or this ledger. **Token:** not held by this session. The WRITE TOKEN block above is left exactly as written and is **not** stale in the sense that needs fixing — it names session `session_01R4yvjTL3XyKCkTPfMcGqiB` and says "held until … this session ends," and that session has ended, so the block's own terms already return the token to Drew. Editing the holder line from an unheld session would be the self-grant the 07-31 corrections exist to prevent; the ledger's convention is that corrections get their own row, which this is. Documentation-only work on a reviewable branch behind a PR — which does not confer the token, per the B-003 precedent above |
| 2026-08-06 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **B-005 / task 1.3 — Week 2 rewritten on the seeded Dataman backlog.** Project selection deleted (proposal doc, four project options, MVP/roles/stories-for-your-own-idea): students now receive `Dataman_Seeded_Backlog.md` and initialize it. File renamed `..._Project_Selection.md` → `..._Backlog_Initialization.md` (git mv, history preserved); no live inbound links broke — the two remaining hits on the old name are dated records in `INGESTION_MANIFEST.md` (points at the frozen `sources/` copy, still correctly named) and `B003_RECONCILIATION_WORKLIST.md`, both left untouched per non-negotiable #8. Header 35 pts unchanged; rubric held at the CONFLICT_LEDGER's decided **10 / 15 / 10** split, with the line-item criteria that ledger deferred to 1.3 now written. **Board columns corrected to the canonical four** (To Do → In Progress → In Review → Done) — the file taught five, including a retired `Backlog` column, and NAMING_CANON §Workflow canon makes those names graded answers. **MoSCoW deliberately not graded here**: Week 4 owns it (Prioritization, 10 pts) and students have not met the stakeholders until Week 3, so Week 2 grades relative estimation (Fibonacci, matching `Sprint_Planning_Template.md`) and board initialization instead; the assignment says so in the student's own words and tells them changing their mind in Week 4 is the skill. Counts verified against the artifact: 26 core stories, 11 in Epics 1–3, 4 stretch bullets. Andrew's Note **rewritten, not migrated** — its subject was project selection, and it carried two fabricated "past student" anecdotes (non-negotiable #5); the replacement is OOC, anecdote-free, and **flagged in-file for instructor sign-off** since it is his voice. Retired 5-column board and the "choose wisely, you'll spend 60–80 hours" framing both gone. Three downstream one-line references updated to stop promising a deleted deliverable (`COURSEMAP.md` Week-2 line, `README.md` module-1 theme + calendar row); the COURSEMAP point figure 25→35 is an early partial execution of an item deferred to task 2.5 and is **recorded as such in the CONFLICT_LEDGER**, with the rest of that block left to 2.5. Single session, no fan-out — Rule 1 spine + Rule 2 voice-sensitive. **Flagged, not fixed:** `sources/uv-285/activities/Module_01_SDLC_Simulation_Lab.md` still builds a library checkout system (B-008 owns it; the in-class contract it must satisfy is stated in an HTML comment in Part 1); the exit-ticket cadence home (`WIRING_MANIFEST` item 6) is a semester-spanning spine call, not a week's; COURSEMAP's Week-1 title and its "KC 6 pts bonus" line are stale in the same way and remain B-014/2.5's. 🔑 **Token — 🚫 not held by this session, 🚫 not self-granted.** The instructor directed this work in-session; a directive to build is 🚫 not a 🔑 grant, and this row does not reproduce it. Documentation-and-content work on a reviewable branch behind a PR, on the same precedent as the B-003 and 2026-08-06 rows above |
| 2026-08-06 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Forecast + patterns filed as findings; `lore/` series established.** A build plan written against a *different* repository (CSC-134: `modules/`, `_lore/`, four CI gates, a numbered findings series) was translated into a work-breakdown forecast for this one. **Nothing built, staged, or promoted** — the document records what the work *would* cost. Produced by a 7-agent workflow under Rule 1a: 5 read-only mappers + 1 synthesis + 1 adversarial verifier = 7 against a cap of 7; **dispatched 7, returned 7, dead 0, empty 0**; 808k subagent tokens, 27.8 min. Every mapper was briefed on non-negotiables #6 and #8 before launch, and the verifier reports **zero** records-read-as-status errors — the class that produced 19 of 20 refutations on 2026-07-29. Verdict **SOUND-WITH-FIXES**: 7 defects named, all 7 corrected before the document was written, and the correction table kept in §6 rather than smoothed away. Two findings are live defects independent of the forecast: **(a)** ADR-003's rehearsal role card + live instructor-as-client brief — 9 reference sites, delegated to task 1.4, re-delegated to 1.3, shipped by neither (B-005 included); the backlog has no mechanism for a closed task's undone remainder. **(b)** `CTS285_COURSE_OUTLINE.md` was marked RETIRE at ingestion and exists nowhere, yet 3 live artifacts still cite it — including `Dataman_Seeded_Backlog.md:410`, which traces the whole stretch-epic block to it, making that artifact set's advertised traceability guarantee uncheckable. Contested count hand-derived: PATHFORM = **39 occurrences outside `sources/`, 22 in `drafts/`, 6 files** (the synthesis said 21 by switching measures mid-sentence). Filed as **`lore/findings/F-001`** (forecast) and **`F-002`** (the recurrence pattern, 12 instances / 6 layers / 7 still open), under a new **`lore/`** series ruled by **`phase0/ADR-011`** (Accepted, instructor-directed): `phase0/` rules · `planning/` plans · `lore/` measures · `drafts/` teaches. The four existing findings-shaped documents were deliberately **not** moved or retro-numbered — they are dated records whose paths are cited elsewhere. No merge gate attached. ADR-011 took the number F-001 had earmarked, so the floor-test lexicon question is now **ADR-012**; the stale reference was corrected in place before merge. **Addendum recorded post-run (§7a):** instructor input that cross-department collaboration is a key course concept and that the design program can supply visual/workflow diagram exemplars — several missing artifacts (Trusted Workflow sequence, struggle-pattern template, ADR-003 role card, board/epic structure) are diagram-shaped rather than prose-shaped, and delivering them through the partnership demonstrates the concept by using it. Recorded as an addendum, **not** folded into the run's output, so what the mappers found stays separable from what was added after. It extends WP-3 from two rails to three (document / person / diagram). **Decides nothing:** it touches the standing GRD-242 blocker, and Rule 8 keeps external parties with a human — no commitment, no schedule, fallback untouched. Two consequences flagged and unruled: a PDF or image is invisible to every mechanical check this repo has or might build (likely rule *source of truth stays text, the diagram renders it* — but that is an ADR), and a partner's work carries its own redistribution question. 🔑 **Token — 🚫 not held by this session, 🚫 not self-granted.** The instructor directed the work in-session; a directive to build is 🚫 not a 🔑 grant, and this row does not reproduce it. Reviewable branch behind a draft PR, same precedent as the rows above |
| 2026-08-06 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **`lore/findings/F-003` — the failing-agent runs.** Scope corrected mid-task by the instructor: the goal is a **findings-and-audit trail for a later researcher**, not a poster brief. Four runs recorded with an explicit evidence-class label each: 2026-03-13 parallel-spine fork (`ABSENT` — agent count, prompts, and detection path unrecorded anywhere), 2026-07-29 76-agent run (`SECONDHAND` — every figure is prose written by the session that ran it; no journal, transcripts, or script survive), 2026-07-31 B-003 (`DERIVED` — checkable because commit `3abc5f4` and the worklist are both in git), 2026-08-06 `F-001` run (`PRIMARY` — run `wf_68ba0468-5c2`, per-agent result and transcript sizes recorded). **Durability defect surfaced:** Rule 1a designates the run journal as *the record*, and the journal is not in version control, is referenced by no file a future session reads, and dies with the container — measured this session, exactly **one** run directory exists on this machine. B-003's evidence survived only because its *work product* was committed. F-003 names the cheapest fix (a committed run record) and **does not rule on it** per `lore/README.md` rule 4. Cross-run observations recorded: death rate tracks wave width (24% at 76, 0% at 7 twice); estimate error is a shape property; the most expensive failure crashed nothing; **a run auditing itself has failed 2 of 2**, while both separately-verified runs held. §4 lists what the record *cannot* answer so absence is not mistaken for evidence. **Presented for a decision, not landed as settled:** instructor asked for a readback before authorizing — land as-is / trim to the translation frame / hold pending the run-record question. Committed to the draft branch as the reviewable path; 🔑 **🚫 not authorization** — CLEAR is the instructor's (ADR-009) |
| 2026-08-06 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **`F-003` trimmed to the translation frame** — instructor chose option 2 of the three run back in the readback, on the reasoning that option 1's detail is *one step back in the trail* rather than lost. Per-run forensic reconstructions collapsed into a single comparable four-row table (date · run · evidence class · estimated · launched · died · outcome · where the evidence is), with mechanisms reduced to one line each. Kept in full: the evidence-class definitions, the five cross-run observations, the durability defect, §5 "what this record cannot answer," and the researcher's note. **Nothing deleted — moved into the history:** the untrimmed version is commit `563b6df`, and the trimmed file says so in a header block so a later reader can retrieve Run 2's full figure table and Run 4's per-agent breakdown. The trim is itself an instance of the fix §4 recommends: the long form survives because the work product was committed, which is exactly why B-003's evidence survived and the other two runs' did not. 🔑 **Token — 🚫 not held, 🚫 not self-granted;** committed to the draft branch as the reviewable path. CLEAR remains the instructor's (ADR-009) |
| 2026-08-06 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Correction — abstract of the 🔑 token hiatus (2026-08-04 → 08-06).** *Carried:* across those three days the holder line named a cloud session that had already ended, so no session held the 🔑 — but the human gate held throughout. Every merge in the window (#40, #43, #45) was performed by the instructor; nothing was self-CLEARed; all work sat on reviewable branches behind draft PRs, per the B-003 precedent where a reviewable path stands in for an unheld token. *Reduced:* the ended holder is cited as `…GqiB`, 🗃️ archived; its grant is reported, not re-quoted. *Standing:* the WRITE TOKEN block and every correction above it are untouched — #42 finding 1 remains a cold-read hazard and is not this row's to rewrite. *Apply when:* the 🔑 passes to a named session, or a human authorises editing the block. **What this does NOT discharge:** the reviewable-path substitute is a precedent, not a ruled policy, and the holder line on `main` can still read live on a cold read even though its own terms have expired. Read together: the system did not act in an undefined manner — it acted under an undefined *record*, which is a different and smaller thing, and the gate that mattered was never the token |
| 2026-08-06 | GitHub Copilot agent | copilot | *(this PR)* | *(this commit)* | **Merge-conflict resolution on `task/adr-011-l0-destination`.** Merged `origin/main` into the branch; carried forward all 2026-08-06 ledger rows without rewriting history; resolved the sole content conflict in `planning/SESSIONS.md` by keeping both sides' append-only rows and defusing the stale/not-stale collision to the narrower cold-read wording above. Renumbered the L0 ruling draft from **ADR-011** to **ADR-012** because `origin/main` now uses ADR-011 for the findings-series ruling. 🔑 **Token — 🚫 not held, 🚫 not self-granted.** Reviewable branch behind a PR; no holder line edited |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **State-truth sweep + `F-004` + two new conventions.** Verified at `1079aba`: **B-005, B-006 and B-007 are all merged — the declared B-004→B-007 spine sequence is closed and all twelve CTS-285 weeks are converted.** `CLAUDE.md:79-84` and `CLAUDE_SUPPLEMENTARY.md:115` both named those three merged tasks as pending; a session planning from either would have redone five weeks. Regenerated both from the tree (non-negotiable #6; `CLAUDE.md`'s header is explicitly "regenerated from the tree", so this is a live-content fix, not a record edit). Backlog rows B-005/006/007 struck through with DONE markers matching the existing `~~B-002~~` pattern; §8 marked complete. **`lore/findings/F-004`** filed rather than editing F-001/F-002 (`lore/README` rule 1, ADR-011 constraint 1): corrects "nine reference sites" → **19 sites across 9 files**, a count reported without its unit — the same measure-switch F-001 §7 documents, now twice by two sessions — and corrects "the ADR rail is absent" → **half-built**, Sprint 3 shipped by B-006 and consumed by Weeks 11–12, missing only the Sprint 1 rung across **2 sites against 2 promises**. F-001/F-002 received dated pointers with original sentences preserved verbatim; **F-002's State column deliberately not flipped**. New: **backlog §7.5 Carried remainders** — the home ADR-003's role card lacked through two delegations — plus a Rule 5 sentence that populates it at FINISH, seeded with the two known orphans (ADR-003 pair; the standup 3-vs-4 reconciliation). New: **`lore/runs/`** run-record series per ADR-011's zone ruling, eleven fields, anti-fields named (no verdict, no score), first record filed. `shared-dataman-artifacts/README.md` stopped claiming to reproduce NAMING_CANON "verbatim" while carrying a superseded pre-ADR-004 two-row table; replaced with a pointer and the three facts the folder needs. **Held unexecuted:** the retired-outline re-point — the instructor's ruling named the DataMon spike, and the spike carries **none** of the four legacy features (`save/load` 0, `battle` 0, `encounter` 0, `collection` 0); `NAMING_CANON.md:26` carries all four. Executing a ruling whose premise this session got wrong would launder the error into the tree, so it is flagged in F-004 §5 and awaits confirmation. 🔑 **Token — 🚫 not held, 🚫 not self-granted;** reviewable branch behind a draft PR, CLEAR remains the instructor's (ADR-009) |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **ADR-003's remainder closed — the role cards and the live client brief exist.** Two rulings received: rehearsal staging is **low-stakes instructor** for 26FA, and live class sessions are **in scope** for the drift corpus because the instructor's own tooling captures transcripts mechanically (which answers the objection that recording mid-scene is a different ask). Authored four instructor-facing artifacts in `shared-dataman-artifacts/`: role cards for Renee, Ms. Alvarez (the **rehearsal** tier per ADR-003:23/:34/:46 — its transcript is not handed out for that round) and Dale, plus `Client_Brief_Instructor_As_Client.md`. **Schema: Zone A playable / Zone B answer key.** Zone A carries behavior (wants in their own words, hard nos, what they have never thought about, three questions that should land and three that shouldn't, an exit); Zone B carries labels (hazard type, conflicts, MoSCoW, entities). **Nothing in Zone A names a hazard type, a conflict, a MoSCoW value, or an ERD entity** — so a peer holding Zone A alone gets the stimulus every student is meant to encounter and none of the answers. Staging is instructor for 26FA, so the split is not load-bearing today; it is what makes peer-pair possible later without redrafting. Every field derives from the three transcripts + `Grading_Key.md` Parts 1–4; **nothing invented**. Register neutral in-world L2, **no new named persona coined** (ADR-003:41, bible §5). Each card states the rule that makes improvisation safe: *an invention does not become a requirement unless the student writes it down.* **The client brief is written to motivate no new core story** — which is what keeps `Grading_Key` guarantee 3 true now that the persona is no longer deferred (ADR-003:48 would otherwise require adding stories); guarantee 3 reworded in both files from "holds by absence" to "holds by construction," and the 750 stays closed. Backlog §7.5 row for the ADR-003 pair marked CLOSED — **explicitly not** closing the `Week_03:103` / `Week_04` promise mismatch, which is a separate content fix. `lore/runs/` gains a classroom-runs section flagging two unruled constraints: student voices in recordings fall under Rule 8, and a transcript of an improvised scene is not automatically course material. 🔑 **Token — 🚫 not held, 🚫 not self-granted;** reviewable branch, CLEAR remains the instructor's |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **`lore/findings/F-005` — open notes filed.** A register of the questions answered informally in conversation, raised and deferred, or discovered mid-task and never written down; a question that lives only in a session's memory is one that gets rediscovered from scratch. **New input recorded with its discussion, as requested: students are planned to speak in character, not only the instructor.** This is the largest item and the one with the most collisions, so it carries the full discussion. What it unlocks: the Zone A/B split stops being hypothetical and becomes load-bearing immediately (the cards were built for it and the commit said it "buys nothing today" — under student play it does); peer-pair is the only staging that gives 24 students more than one turn; it is continuous with a frame students already perform inside. **Seven collisions recorded, none resolved:** (1) it sits alongside the same-day low-stakes-instructor ruling and nothing says whether it supersedes that round or occupies a different one; (2) bible §11 makes persona **opt-in and never graded for depth**, so a student who will not perform needs a path that is not a penalty — an accessibility question, not a formality; (3) if the player is assessed, non-negotiable #1 requires a points-table row, and formative/ungraded is the only resolution that keeps the 750 closed; (4) a student who plays Renee then writes a graded analysis of Renee starts from an uneven position — rotation is one answer, deciding it does not matter is another, discovering it in October is the outcome to avoid; (5) **the client stays the instructor's** — ADR-003:35 makes it the live RSI anchor and a student in that seat collapses the round; (6) the *invention does not become a requirement* rule protects the interviewer but **not** against a player inventing a hard no that contradicts canon — with the instructor playing, canon knowledge was the guard, and under student play that guard is gone; a one-line Zone A addition (*invent freely, but never invent a refusal*) would close it and is unwritten; (7) students performing on record is a different privacy object than an instructor performing on record. **Stated as settled, because the artifacts already implement it:** Zone B never travels to a student under any staging. Register also carries the held re-point, the Week 3/Week 4 promise mismatch, the Canvas M1 delivery gap, the ADR-lite Sprint 1 rung, the orphaned standup reconciliation, the WRITE TOKEN, design-partner scope and rights, reserved ADR-012, the run-record budget overrun, and the moving PATHFORM count. Per `lore/README` rule 4 it **rules on nothing and assigns no owner**; recommendations are marked as recommendations. 🔑 **Token — 🚫 not held, 🚫 not self-granted** |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Role cards rebuilt to levers/buttons/constraints; four rulings applied.** Instructor ruled the card format is **"constraints and buttons and levers (options) rather than 'say stuff'"** — a redesign, not a refinement, so all four cards were rebuilt. Zone A is now a control panel: **LEVERS** (3 dials × 3 positions, set before the scene and held — Forthcoming, a character-specific pressure dial, and one more), **BUTTONS** (trigger → move → rough line, so the player never improvises under pressure and twelve pairs get comparable scenes), **CONSTRAINTS** (hard nos + fixed facts + blind spots). Operating instructions extracted to a new **`Role_Card_HOWTO.md`** so the four cards carry mechanics only and cannot desynchronize — the same defect just fixed in this folder's README. Rulings recorded: **acting is opt-in** (three equal paths — play it, hand it to a voice agent on any phone, or take the interviewer seat); **improv is practice and is not graded**, on LPAA terms; **students never play the client** (unchanged, ADR-003:35); a **voice agent plays the demonstration round** with the instructor interviewing. **The voice-agent path resolves three problems with one decision** — it is the opt-out, the demonstration vehicle, and the reason the control-panel format is right, since a panel is promptable and prose improvisation is not. Reframe recorded: *playing the customer is less roleplay than practice from the other side*, which reads down the accessibility concern without dismissing it. **The invention hole is closed** — every card now routes an unlisted refusal to an *"I'd have to think about that"* button, with the rule stated once in the HOWTO: invent freely, never invent a refusal, because the analyst's graded artifact is downstream. **`LPAA` deliberately not adopted as canon** — it is CSC-134's term, undefined in this repo, recorded as the stated basis of the ruling with a note that importing it needs NAMING_CANON's lexicon lock satisfied first. **Canvas M1 dropped from the plan**: an external markdown→Canvas-HTML skill exists and is tested elsewhere; this repo is not responsible for that implementation and should not build a second path. F-005 gains the rulings as an appended block, questions and answers kept separable. Still open and flagged, not decided: which round students play relative to the instructor's, and rotation so nobody analyses the character they played. 🔑 **Token — 🚫 not held, 🚫 not self-granted** |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Re-point executed (confirmed) + 🗃️ WRITE TOKEN retired.** The instructor confirmed the corrected interpretation, so the held item from `F-004` §5 is now applied across **seven sites**: `Grading_Key.md:114` (guarantee 4) and `:115` (guarantee 5), `shared-dataman-artifacts/README.md:82`/`:83`, `Dataman_Seeded_Backlog.md:410` (the stretch-block header), and the four per-story `↳ Homage to:` lines at `:415-418`. All now cite **`NAMING_CANON.md` §"Dataman vs. DataMon" legacy-game row + ADR-004**, and **name the four features rather than line-numbering them** — a line number into a live canon file moves the first time anyone edits that table, which is how the original defect was built. The spike citation at `:410` was left where it correctly stands (it documents the reskin *direction*, which is what it actually carries). `grep -rn CTS285_COURSE_OUTLINE drafts/` now returns only the two provenance notes recording what the citation used to be — no live dangling reference remains, and the two audit guarantees are checkable again by reading. **WRITE TOKEN:** instructor ruled it was redacted out of the logs and no longer exists; replaceable if needed. Recorded as a **fourth appended correction block**, with the holder line and all three prior corrections kept exactly as written — that history is why the block is legible. Consequence recorded: **the token was a write-serialization device, never the authorization device; authorization is CLEAR (ADR-009) and is untouched.** Losing the token removes a queue, not a gate. Ledger rows stop reporting 🚫-not-held from this row forward, since there is no holdable thing to report on. If re-established: name a **session** never a branch, and reduce the handle rather than republishing a working one — the fix `F-005` §3 proposed on PR #53 and deliberately did not apply, because an unheld session editing the holder line is the self-grant those corrections exist to prevent. **Retirement ruled by the instructor is the legitimate moment**, so it lands as a new block rather than an edit above |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Merged `origin/main` into `task/aug18-state-and-records`; conflict resolved.** Main advanced by five commits while this branch was open (PR #48, the L0 ruling). Sole content conflict in this file: both sides appended ledger rows. **Resolved by keeping all seven — two from main (2026-08-06), five from this branch (2026-08-07) — in date order, nothing rewritten and nothing dropped.** **ADR numbering collided and resolved cleanly in our favour:** PR #53 took ADR-011 for the findings series the same day the L0 draft claimed it, and the parallel session renumbered **its own** draft to ADR-012 rather than ours, then followed the renumber through four already-merged citations (Weeks 6 ×2, 11, 12) that read "L0 per ADR-011". **Consequence for us:** `F-001` WP-2's claim that ADR-012 is the next free number is now false — ADR-012 is the L0 ruling and **the next free is ADR-013**. Corrected in `F-005` rather than edited into the merged `F-001`. That is the **third instance in three days** of a number asserted as free by a document that could not see a parallel branch, which argues "next free ADR" should be read off `phase0/` at write time and never quoted from a finding. **The two token treatments converge rather than collide:** main's 2026-08-06 row filed a *proposed* abstract of the token hiatus whose stated trigger was *"the 🔑 passes to a named session, or a human authorises editing the block"*; this branch's correction block applies it on the second condition, which the instructor's retirement ruling supplies. Two sessions reached the same mechanism independently and neither applied it early. Cross-reference added |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Correction to the row above.** The merge-resolution commit (`97429f2`) claimed two edits that did not land: the token-block cross-reference and the ADR-012→013 correction in `F-005`. A scripted edit failed on a text-match assertion, the shell continued past it because the failing command was not chained to the commit, and **the commit message asserted work the diff did not contain.** Both edits are applied in this commit. Recorded rather than amended away: this is the same defect class as `F-002` — a confident summary outrunning its coverage — produced by the session that filed `F-002`, on the day it filed `F-004` about the same thing recurring. The mechanism is worth naming because it is new: not a stale record this time, but **an unchecked exit status between a scripted edit and the commit that describes it.** Prose discipline does not catch this one; only reading the diff before writing the message does |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **B-008 / task 1.7 — the six in-class activities adapted, and the handouts they promised authored.** Done-when criterion was *each activity points to a real handout and matches the solo Dataman spine*; both halves are now checkable. **Wave:** `bounded-fanout` at exactly the cap — 6 artifact agents + 1 audit + 0 scribe = **7 of 7**. **Dispatched 6, returned 6, dead 0**; no shortfall to report, and the ledger row is hand-written precisely because a scribe would have been the eighth agent the mechanism refuses. Run at **Sonnet** on instructor direction (rate); the SHODANN voice pass was deliberately excluded from the wave and done in this session at Opus tier per Rule 2 — agents were instructed to write **no** SHODANN dialogue and to leave a `SHODANN-SLOT` marker instead, and the audit checked compliance rather than trusting it. **32 markers, 32 filled, 0 residue**, attribution once per file per bible §10. **Built:** six activities in the new `drafts/cts285/activities/` (M01 SDLC Simulation Lab · M02 User Story Workshop · M03 System Design Studio · M04 Sprint Planning Simulation · M05 Code Review Lab · M06 Risk Assessment Workshop), all six at **zero** Dataman references before this work; eight handouts in `activities/handouts/` (Role_Cards_SDLC, SDLC_Phase_Checklist, Story_Quality_Checklist, Design_Review_Checklist, Sprint_Planning_Checklist, Story_Point_Reference, Code_Review_Checklist, Risk_Checklist_Template). **Activities are UNGRADED** — no rows in the canonical points table, and the "points" in M02/M04 are **story points**, estimation not grade; that removes the arithmetic error class that dominated B-006/B-007, and the agents were told so explicitly rather than left to infer it. **Two merged weeks constrained two agents and were named as hard constraints in their prompts:** M05 had to teach Week 9's *comparative* review (same stories, same manual, so behavior is comparable; manual-silent disagreement is an ADR subject and never a filed defect) and M06 had to use Week 11's four verdicts verbatim (Resolved / Mitigated / Accepted / Materialised, with *Materialised is not a failure grade* preserved). The audit confirmed both against the merged files rather than against the agents' reports. **Written outside the wave, by this session:** `handouts/Incident_Reports_Week11.md` — the artifact Week 11's incident exercise hands out, in three variants each engineered for a different one of the three legitimate outcomes that week names (A a real defect, with the reproduction condition buried in an aside the reporter dismissed; B **no fault at all** — a decision made in Sprint 2, defensible, never written down; C cannot be reproduced, which Week 11 grades as correct). Adding it as a seventh artifact would have breached the cap, which is the mechanism working rather than an inconvenience. **Two defects found and fixed after the audit returned clean, and both are worth naming because the audit missed them:** (1) `Week_11`'s incident section presumed a single variant — *"this one, if real, touches the one behavior with no acceptable failure mode"* — which is false for variants B and C, so the wording is now variant-neutral and the severity-vs-tone mismatch is taught explicitly; the same edit wires the handout in via an instructor-only comment, since nothing referenced it. (2) **Nested HTML comments in 8 of the 14 new files** — a `<!-- PATHFORM: pending spine ruling -->` marker placed *inside* a provenance `<!-- ... -->` block, where the inner `-->` closes the outer comment and the remainder of the header renders as visible page text. Fixed by de-nesting to `[PATHFORM: pending spine ruling]`, which stays greppable on the same string, so ADR-006's marker survives. **Checked and clean elsewhere: 0 pre-existing instances** in `drafts/`, `phase0/`, or `planning/` — the bug was introduced by this wave and does not exist in merged material. Provenance headers were **appended to, never rewritten** (non-negotiable #8): the first-pass line saying no SHODANN was written is correct as the record of that pass, and a second line records the voice pass beneath it. `sources/` byte-identical throughout — `git status --short sources/` empty at every checkpoint. Also carried on this branch: a fix to `.claude/workflows/bounded-fanout.mjs` so a stringified `args` parses instead of refusing with *"no artifacts given"* — the cap, the constant, and the refusal path are untouched; the change only stops the transport from firing a refusal that the plan did not earn. **Flagged, not fixed:** M04 writes story points as `5 pts`, which collides with the grade-point convention `X pts` used everywhere else — unambiguous in context ("16 story points", "within 16 capacity") but a hazard for any future mechanical points sweep. 🔑 **Token — 🚫 not held by this session, 🚫 not self-granted.** The instructor directed this work in-session; a directive to build is 🚫 not a 🔑 grant, and this row does not reproduce it. Reviewable branch behind a draft PR; CLEAR remains the instructor's (ADR-009) |
| 2026-08-07 | GitHub Copilot agent | copilot | *(this PR)* | *(this commit)* | **Merge-conflict resolution on `task/1-7-b008-activities`.** Merged `origin/main` into the branch and resolved the sole content conflict in `planning/SESSIONS.md` by keeping main's appended ledger rows, preserving the branch's B-008 row, and leaving earlier records untouched. The merge also carried forward main's retirement of the 🔑 token, so this branch's newly landed row omits the retired hold/not-held footer. |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Correction to the merge two rows above, and Copilot's #56 review applied.** **(1) The merge kept every row and edited one.** Copilot's `1f9b0b6` reported *"kept the appended ledger rows on both sides"*, and on a row count that is true — 30 ancestor + 9 from main + 1 from this branch, none dropped, no duplicates. What the count does not show is that the **B-008 row itself was altered**: a 185-character span was deleted from it, and the span was the 🔑 **WRITE TOKEN** clause — *"🔑 Token — 🚫 not held by this session, 🚫 not self-granted. The instructor directed this work in-session; a directive to build is 🚫 not a 🔑 grant, and this row does not reproduce it."* That clause exists for exactly one reason: to make a row unable to be read later as evidence that the session held write authority. Deleting it leaves the row **silent** on the token, which is the ambiguity the 2026-07-31 correction and the `semantics-preserved-abstract` skill were both written to prevent. **Restored verbatim.** Restoring text a merge removed from a row is repair, not a rewrite — the row now says what its session wrote — and this correction row records that the interval existed, per this ledger's convention. **The lesson generalises past this incident:** an append-only ledger can be damaged by a merge that drops nothing, so *"were any rows lost"* is the wrong check. The check is a diff of the rows themselves. Row counts agreed at every step here and the defect was still there. **(2) Copilot's review was right, and larger than it sampled.** It found the manual citations mixing two page-numbering schemes. Verified against `reference/dataman/DATAMAN_MANUAL_TRANSCRIPT.md`, which labels every section **`PDF p.X (printed p.Y)`** — and the wave's output used bare numbers from *both* schemes, sometimes in one sentence (`Code_Review_Checklist.md` cited two-tries as "pp.4–5" (PDF) and the memory-bank cap as "p.4" (printed) on consecutive lines). **One citation was flatly wrong under either reading:** M06 sourced the ten-problem ceiling to "p.19" — printed p.19 is *Hints for Parents and Teachers*, and PDF p.19 is **untranscribed**. The cap is at PDF p.6 (printed p.4), restated at PDF p.23 (printed p.21). This is not cosmetic: **Weeks 9 and 11 grade students on citing manual pages**, and issue #18 requires anything graded to be answerable from PDF pp.1–6 and 21–26 — a graded exercise pointing at an untranscribed page is uncompletable. Every citation across the wave's output converted to the transcript's own dual form; the ambiguous-single-page count is now **zero**. Fixed in **two files Copilot did not sample** as well — `Design_Review_Checklist.md` carried the same mixed scheme on two lines. **(3) Story points disambiguated.** M04's `5 pts` → `5 story points`, which the B-008 row flagged as *"flagged, not fixed"* and Copilot independently reached; the only `N pts` left in `activities/` is a genuine grade-point cross-reference to Week 9's parts. 🔑 **Token — 🚫 not held, 🚫 not self-granted;** CLEAR remains the instructor's (ADR-009) |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **B-009 / task 1.9 — the 26FA instructor guide.** Adapted from the frozen `sources/uv-285/instructor-guide/INSTRUCTOR_GUIDE.md` (583 lines) into `drafts/cts285/instructor-guide/` (new category), 827 lines. Single session, **no fan-out** — one document in one voice, and `bounded-fanout` would refuse it anyway (one file, one owner). **Two of the backlog line's three clauses did not match the file, and both are recorded in issue #58 rather than silently worked around.** (a) *"Remove QM doctrine"* is a **no-op**: `grep -iE 'quality matters|\bQM\b'` against the source returns **zero hits**; there was never any QM in this file. (b) *"Keep RSI actions"* is **net-new authoring, not preservation**: the source also has **zero** RSI references, while the done-when criterion requires RSI actions per module. Written from `QM_Retirement_Note.md`'s surviving-mechanism list and **ADR-003**'s stakeholder ladder, as a standalone §3 plus per-module actions in §4. **Scope call, stated not assumed: Modules 4, 5 and 6 are delivered complete** (net-new, written against the merged Week 07/09/11/12 assignments); **Modules 7 and 8 are explicit placeholders** naming their blocking B-IDs — weeks 13–16 are B-010…B-013, not started, so there are no assignments to teach from and inventing notes for them would be fabrication. M8 additionally carries the standing GRD-242 consult blocker and says so. So B-009's criterion is met for 3 of the 5 modules it names; **the shortfall is stated, not filtered.** Modules 1–3 re-anchored only where the source contradicted the spine — *"use YOUR sample project"*, *"improvise based on their project"*, and a struggle pattern whose signs were *"I'm building the next Facebook"* — because a guide that coaches project selection while the course runs one shared project is self-contradicting. **Canon defects fixed rather than carried:** §7 *"The AlgoCratic Theme (Optional Use Guide)"* called the frame detachable satire, told new instructors **not** to use it, and illustrated it by quoting the **retired punitive register** (*"CLEARANCE LEVEL RED…"*); **rewritten, not migrated** (non-negotiable #3) into §8 "The Two Layers", which teaches Futures/Media as layers per `KAYFABE_ARCHITECTURE.md`, states SHODANN's L1 constraints, and keeps the one clause that was already correct and load-bearing — **theme engagement earns zero credit, ever**. **"Sacred Flow"** → **Trusted Workflow** with the eight ADR-007 verbs, and the guide states plainly that **CLEAR is performed by the instructor, never the student** (ADR-009). **40/40/20 reframed** per ADR-002 §4 from a grading formula into a course-level philosophy, with the explicit instruction to stop if you find yourself making one rubric sum to it — that reading was the largest source of points conflicts in the refresh. Institution rendered **FTCC** only (non-negotiable #9). The source's named instructional-design contact is **held in an in-file sign-off comment, not carried and not silently deleted** — `QM_Retirement_Note.md` records her name appearing on generated audit work she did not perform, so whether she is a live 26FA contact is an instructor call. Stale footer claim *"60+ pages | Version 1.0 | January 2026"* dropped (non-negotiable #6). **Arithmetic verified against the canonical table:** module totals 75/75/100/100/100/100/70/80 = 700, **excluding** the 50 in knowledge checks, which is the course map's own convention — and the guide says so explicitly, because an instructor reading module totals alone would otherwise miss five graded quizzes. KCs stated as **graded, not bonus**, with the retired *"6 pts bonus"* figure named as retired so it is not propagated from the still-stale COURSEMAP line (B-014/2.5's to fix, deliberately not touched here). **Checks:** 27 struggle blocks, all four template fields matched 27/27; valid UTF-8, no mojibake; **zero nested HTML comments** — the B-008 defect was checked for by construction this time rather than discovered afterwards; `sources/` untouched; `phase0/` untouched. **BLOCKED-ON, and it is a merge-order fact rather than a defect:** the guide cites **eight** activity and handout files that live in **PR #56 (B-008)** and are not on main. Every reference is correct against the post-#56 tree; **#56 merges first.** The eight are named in the file's own provenance header so a later reader does not have to re-derive them. 🔑 **Token — 🚫 not held by this session, 🚫 not self-granted.** The instructor directed this work in-session; a directive to build is 🚫 not a 🔑 grant. Reviewable branch behind a draft PR; CLEAR remains the instructor's (ADR-009) |
| 2026-08-07 | Cloud Claude Code (remote) | Claude (this session) | *(this PR)* | *(this commit)* | **Addendum to the B-009 row above — merged `origin/main`, and the RSI section now cites artifacts that did not exist when it was written.** The row above stands as written; this is its correction row, per this ledger's convention. `origin/main` moved nine rows and eight commits while B-009 was in flight, including **#48** (ADR-012) and **#55**. The `SESSIONS.md` conflict was a pure **append collision at the tail of an append-only ledger** — nine rows from main against one from this branch — and **both sides were kept**, main's first (they landed first) and this branch's last. Picking a side is the wrong answer here and it is the wrong answer that looks right; a resolver that "resolves" an append-only ledger by choosing a hunk silently destroys the record the file exists to hold. **Substantive change, not just a merge:** `0fa6e5c` and `ddce6f1` landed **ADR-003's role cards and live client brief** — the remainder `lore/findings/F-001` recorded as delegated to 1.4, re-delegated to 1.3, and shipped by neither. §3 of the guide was written assuming those artifacts were still pending, describing the rehearsal staging as the instructor's to invent. They exist, so §3 now cites `Role_Card_{Parent_Renee,Teacher_Alvarez,Collector_Dale}.md`, `Client_Brief_Instructor_As_Client.md`, and `Role_Card_HOWTO.md`, and carries the three things in the HOWTO that change how the week runs and are not visible from the cards alone: the cards are **control panels** (levers/buttons/constraints) rather than scripts, **acting is opt-in with three equal paths** including handing the card to a voice agent as a first-class option rather than an accommodation, and **playing the customer is not the soft version** of the exercise. The §2 prep checklist gains the HOWTO and the brief, with the brief marked instructor-only. **Naming ambiguity flagged, not fixed:** two unrelated artifact families are now both called role cards — `drafts/cts285/activities/handouts/Role_Cards_SDLC.md` (B-008, the four SDLC *team* roles in the Module 1 lab) and `drafts/cts285/shared-dataman-artifacts/Role_Card_<Stakeholder>.md` (ADR-003, *stakeholder* personas). Different directories, different concepts, no filename collision and no defect — but a naming sweep or a search for "the role cards" will find both, and whoever writes Modules 7–8 should know that before citing either. **Link state unchanged:** the same eight `drafts/cts285/activities/**` references remain unresolved on this branch and only those eight; they are **PR #56**'s files and resolve when it merges. **#56 was left alone deliberately** — its conflict was in flight and the instructor had directed a different agent to it, so this session aborted its own in-progress merge there rather than race a second writer into one branch. 🔑 **Token — 🚫 not held, 🚫 not self-granted** |
